-# %h2 Advice
-# - @advice.each do |advice|


-# %h2 New advice
-# = form_for(AdvicePost.new)



%h1 Listing advice

- @advice_posts.each do |advice_post|
	%h4= "Title: #{advice_post.title}"
	%p= "Body: #{advice_post.body}"
	- if advice_post.image?
		%p Image:
		= image_tag advice_post.image.url(:thumb)
	- elsif advice_post.video_html then
		%p Video:
		= advice_post.video_html.html_safe
	= link_to "up", vote_advice_post_path(advice_post, type: "up"), method: "post"
	= link_to "down", vote_advice_post_path(advice_post, type: "down"), method: "post"
	%p= pluralize advice_post.reputation_for(:votes).to_i, "vote"
	%p 
		= link_to 'Show', advice_post
		= link_to 'Destroy', advice_post, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

%h1 New advice
= render partial: 'form', locals: {advice_post: AdvicePost.new}
